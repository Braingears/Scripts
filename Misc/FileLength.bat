Need to navigate to the directory before running this command. This command runs in POWERSHELL. 
cmd /c dir /s /b |? {$_.length -gt 260}

StorageCraft ShadowProtect

cmd /c dir /s /b |? {$_.length -gt 258}
